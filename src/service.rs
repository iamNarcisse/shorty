pub struct Service {
    /// The name of the service.

}


impl Service {
    /// Creates a new service.
    pub fn new() -> Service {
        Service {
        }
    }

    fn store(&self, link: String) -> String {
        link
    }
}